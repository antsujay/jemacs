# taskctl (jemacs example)

Stdlib-only Go CLI for editor user studies. Multi-file, has tests, has a
`go build`-able main.

```
go build ./...
go test ./...
./taskctl add -p high "fix the bug"
./taskctl ls
```
